export interface modelInterface {
	searchQuery: string;
	searchLimit: number;
	sortOrder: string;
	localSortKey: string;
}

export interface headerInterface {
	name: string;
	clickFunc: string;
	text: string;
	showBtn: Boolean;
}
